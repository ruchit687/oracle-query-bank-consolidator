# oracle-query-bank-consolidator
Python accelerator that runs a batch of SQL queries maintained in an Excel query bank against an Oracle database and consolidates results into a deduplicated summary.
